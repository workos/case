# Case harness marker files (created by /case skill, cleaned up by hooks)
.case-active
.case-tested
.case-manual-tested

# Local project manifest (copy from projects.example.json and customize paths)
projects.json
